{% extends 'base.html.twig' %}

{% block title %} Shoes Details - Lassap Shop {% endblock %}

{% block stylesheets %} <link rel="stylesheet" href="{{asset('css/shoes-details.css')}}"> {% endblock %}

{% block body %}
    <section id="shoes-details">
        <div class="details-page">
            <div class="shoes-picture">
                <img src="{{asset('img/')}}{{product.image}}" alt="{{product.model}}">
            </div>
            <div class="shoes-info">
                <div class="shoes-data">
                    <h2 class="brand-name">{{product.getBrand().getName()}}</h2>
                    <p class="shoes-model">{{product.Model}}</p>
                    <p class="shoes-price">£{{product.price}}</p>
                    <p class="shoes-description">
                        {{product.description}}
                    </p>
                </div>
                <form id="add-shoes-cart" method="post" action="{{path('add_to_shoes', {id: product.id})}}">
                
                    <button type="submit">Add to Cart</button>
                </form>
            </div>
        </div>
    </section>
{% endblock %}
